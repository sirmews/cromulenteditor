id: no-empty-catch
category: error-handling
severity: warning
language: TypeScript
message: "Empty catch block without explanation. Add a comment explaining why errors are ignored."
rule:
  regex: 'catch\s*(?:\([^)]*\))?\s*\{[\s]*\}'
  kind: catch_clause
files:
  - 'src/**/*.ts'
  - 'src/**/*.tsx'
